'use strict'

class cacheManager {
    constructor() {
        this.config = []
    }

    addConfig () {
        this.config.push({
            
        })
    }
}

const CacheManager = new cacheManager()